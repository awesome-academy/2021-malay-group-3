<%= form_for @course do |f| %>
<%= render "shared/error_messages", object: f.object %>
<div class="field">
<%= f.text_area :course_name, placeholder: t(:create_new_course) %>
</div>
<%= f.submit t(:create), class: "btn btn-primary" %>
<% end %>
